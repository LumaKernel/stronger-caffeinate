## Versioning

- Bump **major version** every commit (task/commit).
- Version is defined in `Sources/stronger-caffeinate/stronger_caffeinate.swift` as `static let version` in `StrongerCaffeinate`.
- When committing, always update the version.
